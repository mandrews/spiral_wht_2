Milestones:

  - External interface
  - Build suffix for different types

  - Interal and External Doxygen documentation targets
    - Naming convention cleanup
    - indent

  - Simple ctest and check targets

  - Weights input

  - C Version of DP
  - Retooled version of GA

  - Retool old versions of whtgen
  - Perl or C version of whtgen
    - Should accept sse and sse2 flags (shuf is platform specific)

  - C Version of best_trees store
    - Configure / CPUID info in header

Bugs:

  - Some minor memory leak issues with wht_very and wht_randtree

  - libtool, bison, flex, ruby dependencies not checked
    - check in maintainer mode only ?

  - Infinite loop in dynamic programming when max-unrolled-size < 3


Issues:

  - Can we store left and right I sizes on W
    - Will remove need to call guard

  - Can we store stride on W


